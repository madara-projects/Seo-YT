import { NotMigrated } from "./NotMigrated";

export default function SettingsPage() {
  return (
    <NotMigrated
      title="Settings"
      description="Channel connection, tokens, and system status."
      legacyHash="#settings"
    />
  );
}
