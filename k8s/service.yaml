apiVersion: v1
kind: Service
metadata:
  name: seo-app-service
  namespace: seo-app
  labels:
    app: seo-app
spec:
  type: NodePort
  selector:
    app: seo-app
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 8000
      nodePort: 30007
